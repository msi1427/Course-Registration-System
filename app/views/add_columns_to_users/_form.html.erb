<%= form_with(model: add_columns_to_user, local: true) do |form| %>
  <% if add_columns_to_user.errors.any? %>
    <div id="error_explanation">
      <h2><%= pluralize(add_columns_to_user.errors.count, "error") %> prohibited this add_columns_to_user from being saved:</h2>

      <ul>
      <% add_columns_to_user.errors.full_messages.each do |message| %>
        <li><%= message %></li>
      <% end %>
      </ul>
    </div>
  <% end %>

  <div class="actions">
    <%= form.submit %>
  </div>
<% end %>
